name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-dashboard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: dashboard/package-lock.json
      - run: cd dashboard && npm ci
      - run: cd dashboard && npx tsc --noEmit --skipLibCheck
      - run: cd dashboard && npm run build

  test-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pytest numpy pandas scikit-learn xgboost pyyaml
      - run: cd prediction && python -m pytest tests/ -v --tb=short || true
      - run: cd simulator && python -c "from charger import Charger, ChargerProfile; from fleet import Fleet; print('Imports OK')"
